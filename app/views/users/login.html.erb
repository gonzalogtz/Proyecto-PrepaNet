<!DOCTYPE html>
<html lang="en" class="azulArmando">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="favicon.ico">

    <title>Login - SST</title>

    <!-- Bootstrap core CSS -->
    

    <!-- Custom styles for this template -->
    <link href="<%= asset_path("login.css.scss")%>" rel="stylesheet">
    <link href="<%= asset_path("cover.css.scss")%>" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body class="azulArmando">
    <div class="site-wrapper">

      <div class="site-wrapper-inner">

        <div class="cover-container">
          <div class="inner cover">
            <img class="logo-cover" src="<%= asset_path("logoPrepa.png")%>" alt="Logo de Prepanet" />

            <div class="form-signin">
              <h2 class="form-signin-heading">Iniciar Sesión</h2>
              <%= text_field_tag :userid, "", class: "form-control", placeholder: "Clave de Usuario", required: true %>
              <br>
              <%= password_field_tag :password, "", class: "form-control", placeholder: "Contraseña", required: true  %>
              <p id="error_credenciales" style = "display: none"></p>
              <br>
              <%= submit_tag "Iniciar Sesión", class: "btn btn-lg btn-default btn-block", id: "login_button" %>
            </div>

          </div>

          <div class="mastfoot">
            <div class="inner">
              
            </div>
          </div>

        </div>

      </div>

    </div>

  </body>
</html>